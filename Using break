#include <stdio.h>
int main() {
    int i;
    for (i = 1; i <= 100; i++) {
        if (i % 7 == 0) {
            printf("The first number divisible by 7 is: %d\n", i);
            break; 
        }
    }
    return 0;
}
