#include <stdio.h>
int main() {
    int arr[3] = {10, 20, 30};
    int *p = arr;
    printf(" %p, value = %d\n", p, *p);
    p++;  
    printf("After p++ : %p, value = %d\n", p, *p);
    p--;  
    printf("After p-- : %p, value = %d\n", p, *p);
    return 0;
}
