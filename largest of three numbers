#include<stdio.h>
int largest(int a,int b,int c) {
    if(a>=b && a>=c)return a;
    if(b>=a && b>=c)return b;
}
int main(){
    int a,b,c;
    printf("enter three numbers");
    scanf("%d%d%d",&a,&b,&c);
    printf("largest=%d\n",largest(a,b,c));
    return 0;
}
