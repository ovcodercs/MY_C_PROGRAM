#include <stdio.h>
enum Day {
    Sunday = 1,
    Monday,
    Tuesday, 
    Wednesday,
    Thursday,
    Friday,
    Saturday
};
int main() {
    enum Day today;
    today = Saturday;
    printf("Today is day number: %d\n", today);
    return 0;
}
