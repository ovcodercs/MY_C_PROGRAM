#include <stdio.h>
int main() {
    int a, b, product;
    printf("Enter first number: ");
    scanf("%d", &a);
    printf("Enter second number: ");
    scanf("%d", &b);
   product = a * b;
    printf("Product of %d and %d is %d\n", a, b, product);
    return 0;
}
